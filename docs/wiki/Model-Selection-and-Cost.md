# Model selection and cost

Smart, Quality, Fast, Economy, and Manual modes route by capability. Every automatic decision shows the model, reason, lifecycle badge, constraints, maximum estimated cost, and compatible fallback policy.

Fallback is permitted only for rate limiting or model unavailability, never for authentication, quota, safety, or invalid controls. Resolution, duration, aspect ratio, audio, and reference behavior are never silently changed.
