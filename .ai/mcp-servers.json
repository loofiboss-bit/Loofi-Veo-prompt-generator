{
  "$schema": "https://veo-studio.dev/schemas/mcp-servers.json",
  "_comment": "Canonical MCP server definitions — synced from .workspace/config.json. Run: node scripts/sync-workspace.mjs",
  "servers": {
    "docker-mcp": {
      "command": "docker",
      "description": "Docker Desktop MCP Toolkit — Context7, Fetch, Filesystem, Gemini API Docs, GitHub, Memory, Playwright, Puppeteer, Sequential Thinking, Time",
      "args": [
        "mcp",
        "gateway",
        "run"
      ],
      "env": {}
    },
    "loofi-agent-server": {
      "type": "http",
      "description": "Loofi AI Agent Server — local MCP endpoint for workspace, training, observability, and ops tools",
      "url": "http://127.0.0.1:8013/mcp",
      "headers": {
        "Authorization": "Bearer ${LOOFI_AGENT_API_TOKEN}"
      },
      "vscode": {
        "headers": {
          "Authorization": "Bearer ${env:LOOFI_AGENT_API_TOKEN}"
        }
      },
      "opencode": {
        "headers": {
          "Authorization": "Bearer {env:LOOFI_AGENT_API_TOKEN}"
        }
      },
      "tools": [
        "*"
      ]
    }
  }
}
