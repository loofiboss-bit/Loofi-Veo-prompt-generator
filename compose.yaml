services:
  veopromptgenerator:
    image: veopromptgenerator
    build:
      context: .
      dockerfile: ./Dockerfile
      target: production
    environment:
      NODE_ENV: production
    ports:
      - 8080:8080
