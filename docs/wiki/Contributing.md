# Contributing

Use short-lived branches and conventional commits:

```bash
git checkout -b feat/flow-veo-export
npm run validate
git commit -m "feat(flow-veo): add export mode"
```

Pull requests should include tests, docs, and screenshots when user-facing UI
changes.
