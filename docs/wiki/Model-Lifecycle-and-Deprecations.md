# Model lifecycle and deprecations

The versioned model catalog is the only executable source for provider IDs, API surfaces, capabilities, lifecycle, pricing date, regions, replacement, sunset, and minimum SDK version.

Preview models may change. Shut-down models fail the release gate and cannot enter routing or fallback paths. Unknown future IDs are preserved during migration for recovery diagnostics.
