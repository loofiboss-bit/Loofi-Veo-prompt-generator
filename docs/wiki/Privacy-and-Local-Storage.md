# Privacy and local storage

Projects and media are local by default. Desktop credentials remain in the OS vault and are never returned through preload. Provider execution, paid job state, download, hashing, and file writes occur in Electron main.

Support bundles redact keys, authorization headers, URL credentials, and prompt content. Ollama endpoints are restricted to loopback HTTP.
